import styled from 'styled-components';
import { BrandColor } from '../../styled-components/GlobalStyles';

const StyledLine = styled.hr`
  background-color: ${BrandColor.PINKISH_GREY};
`;

export { StyledLine };
